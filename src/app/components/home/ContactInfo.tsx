import MediaIconBar from "../common/MediaIconBar";

export default function ContactInfo() {
  return (
    <footer className="contact-info w-full h-[25rem] box-border px-[6.25rem] pt-28">
      <MediaIconBar type="yellow" gutter="lg" />
    </footer>
  );
}
